language: generic 
sudo: required
services:
  - docker

before_install:
  - docker build -t gregoriusjimmy/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true gregoriusjimmy/docker-react npm run test